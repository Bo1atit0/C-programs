#include <stdio.h>

/**
* main - print string to stdout.
*
* Returns: 0 if successful.
*/

int main(void)
{
  char str[] = "with proper grammar, but the outcome is a piece of art,";
  printf("%s\n", str);
  return (0);
}